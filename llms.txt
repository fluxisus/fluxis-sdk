# Fluxis — Crypto Payment SDKs

> Fluxis is a crypto payment processing platform that standardizes payment instructions
> via the NASPIP protocol. Merchants integrate Fluxis to accept cryptocurrency payments.
> This is the index for all available SDK integration guides.

## Available SDKs

- [TypeScript SDK](packages/backend/sdk/llms-full.txt): `@fluxisus/sdk` — Node.js 18+, zero dependencies, native fetch
- [C# SDK](packages/backend/sdk-csharp/llms-full.txt): `Fluxis.Sdk` — .NET 6/8, async/await, System.Text.Json

## Frontend SDKs (in development)

Browser/UI packages under `packages/frontend/` — see [`packages/frontend/README.md`](packages/frontend/README.md). No `llms-full.txt` yet.

## Common Concepts (all SDKs)

### Authentication
Two-step auth — all SDKs handle this automatically:
1. `POST /v1/auth/token` with `api_key` + `api_secret` → PASETO v4 access token
2. All requests send dual headers: `Authorization: Bearer <token>` + `x-fluxis-api-key: <api_key_id>`

### Environments
| Environment | Base URL | API Key Prefix |
|-------------|----------|----------------|
| Staging | `https://api.stgfluxis.us/v1` | `fxs.stg.*` |
| Production | `https://api.fluxis.us/v1` | `fxs.prd.*` |

### Payment Flows
1. **Direct crypto** (`createPaymentRequest`): You specify a `unique_asset_id`. Returns a NASPIP token (render as QR).
2. **Hosted checkout** (`createPaymentRequestCheckout`): You specify a `coin_code` (fiat for display). Returns a checkout URL.
3. **Payment intention** (`createPaymentIntention`): For `cashier_open` PoS — merchant sets amount, payer selects currency.

### Unique Asset IDs
Format: `n{network}_t{tokenAddress}`. Example: `npolygon_t0x3c499c542cEF5E3811e1192ce70d8cC03d5c3359` = USDC on Polygon.

### Webhooks (account-scoped)
Webhooks are configured per account (not per PoS). Event types: `payment_request`, `incoming_transfer`, `refund`.
HMAC-SHA256 signature verification via `verifyWebhookSignature()`. The webhook `secret` is returned once when creating a webhook.

### Payment Request Statuses
`pending`, `created`, `processing`, `completed`, `confirmed`, `overpaid`, `underpaid`, `expired`, `failed`

### Point of Sale Types
`cashier_fixed`, `online_fixed`, `cashier_open`

### Transaction Types
`deposit`, `withdraw`, `refund`, `adjustment`, `swap`, `payment_in`, `payment_out`, `dry_run`

### Pagination
`/transactions` and `/pos` use `page` + `page_size`. Responses include `page`, `page_size`, `total`, `total_pages`.

### API Response Envelope
```json
{ "status": "success", "data": { ... } }
{ "status": "error", "code": "AK0001", "message": "...", "details": "..." }
```
